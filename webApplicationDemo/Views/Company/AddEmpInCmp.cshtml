@{
    ViewData["Title"] = "Add Employee";
    int companyId = ViewBag.CompanyId;
    string companyName = ViewBag.CompanyName ?? "Unknown Company";
}

<div class="container mt-4">
    <h2 class="text-center mb-4">Add New Employee to <span class="text-primary">@companyName</span></h2>

    <form asp-action="AddEmployee" asp-controller="Employee" method="post" enctype="multipart/form-data">
        <input type="hidden" name="CompanyID" value="@companyId" />  <!-- Hidden CompanyID -->

        <div class="mb-3">
            <label for="FullName" class="form-label">Full Name</label>
            <input type="text" class="form-control" id="FullName" name="FullName" required>
        </div>

        <div class="mb-3">
            <label for="Email" class="form-label">Email</label>
            <input type="email" class="form-control" id="Email" name="Email" required>
        </div>

        <div class="mb-3">
            <label for="Phone" class="form-label">Phone</label>
            <input type="text" class="form-control" id="Phone" name="Phone" required>
        </div>

        <div class="mb-3">
            <label for="HireDate" class="form-label">Hire Date</label>
            <input type="date" class="form-control" id="HireDate" name="HireDate" required>
        </div>

        <div class="mb-3">
            <label for="EmployeeImageFile" class="form-label">Employee Image</label>
            <input type="file" class="form-control" id="EmployeeImageFile" name="EmployeeImageFile" accept="image/*">
        </div>

        <button type="submit" class="btn btn-primary">Add Employee</button>
    </form>
</div>
